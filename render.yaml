services:
  - type: web
    name: climaneer-dashboard
    env: node
    region: singapore
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_WS_URL
        value: https://climaneer-server.onrender.com
